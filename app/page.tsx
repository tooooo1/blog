const HomePage = () => (
  <article className="w-full max-w-2xl">
    <h1 className="text-2xl font-medium">기록</h1>
    <p className="mt-2 text-[color:var(--muted)]">가볍게 쓰고 쉽게 읽는 공간</p>
  </article>
);

export default HomePage;
