node_modules
.DS_Store
.vercel
.next
.env
.env.local

# OMC (oh-my-claudecode) local state — not for version control
.omc/
.claude/settings.local.json

# Playwright/browser test artifacts
.playwright-mcp/

# Next.js generated types (regenerated per env; do not commit)
next-env.d.ts

# Local scratch workspace
_workspace/

# Serena (Codex MCP) local state
.serena/
